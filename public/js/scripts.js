document.addEventListener('DOMContentLoaded', () => {
    // Example: Show a message when the page loads
    console.log('Custom Activity loaded.');

    // Optionally, set up event listeners or initialize UI components here
});